# MemoCue 生产环境配置（VPS子目录部署）

# 子目录路径（重要！Nginx 反向代理需要）
BASE_PATH=/memocue

# 服务端口（建议使用 3001 避免与其他服务冲突）
PORT=3001

# 时区
TZ=Asia/Shanghai

# 加密密钥（请修改为随机值）
ENCRYPTION_SECRET=请替换为32位以上随机字符串

# 日志级别
LOG_LEVEL=info

# 数据目录（使用默认即可）
DATA_DIR=./data

# 生产环境标识
NODE_ENV=production

# HTTPS 安全策略（HTTP 环境设为 false，HTTPS 环境设为 true）
ENFORCE_HTTPS=false